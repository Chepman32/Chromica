# Filter System Architecture

## System Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         User Interface                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           FilterToolModal.tsx                            │  │
│  │  ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐ ┌────┐             │  │
│  │  │ BW │ │Sepia│ │Vint│ │Cool│ │Warm│ │🔒  │  ...        │  │
│  │  └────┘ └────┘ └────┘ └────┘ └────┘ └────┘             │  │
│  │                                                           │  │
│  │  Intensity: [30%] [50%] [70%] [100%]                     │  │
│  │                                                           │  │
│  │  [Apply Filter]  [👑 Unlock Premium]                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │ onApply(filterId, intensity)
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                        State Management                         │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           useEditorStore (Zustand)                       │  │
│  │                                                           │  │
│  │  appliedFilter: {                                        │  │
│  │    id: 'sepia',                                          │  │
│  │    type: 'sepia',                                        │  │
│  │    intensity: 0.8                                        │  │
│  │  }                                                       │  │
│  │                                                           │  │
│  │  applyFilter(filter) → updates state + history          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │ appliedFilter
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                      Filter Processing                          │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           filters.ts                                     │  │
│  │                                                           │  │
│  │  getFilterById('sepia')                                  │  │
│  │    ↓                                                     │  │
│  │  filter.getColorMatrix(0.8)                              │  │
│  │    ↓                                                     │  │
│  │  lerpMatrix(identity, sepiaMatrix, 0.8)                  │  │
│  │    ↓                                                     │  │
│  │  [0.393, 0.769, 0.189, 0, 0, ...]  (20 values)          │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           colorMatrix.ts                                 │  │
│  │                                                           │  │
│  │  getFilterColorMatrix(appliedFilter)                     │  │
│  │    ↓                                                     │  │
│  │  BASE_FILTER_MATRICES['sepia']                           │  │
│  │    ↓                                                     │  │
│  │  composeMatrices(                                        │  │
│  │    saturationMatrix(0.4),                                │  │
│  │    contrastMatrix(1.05),                                 │  │
│  │    rgbScaleMatrix(1.05, 0.95, 0.82)                      │  │
│  │  )                                                       │  │
│  │    ↓                                                     │  │
│  │  mixMatrices(identity, matrix, intensity)                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │ colorMatrix (20 numbers)
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                         Rendering                               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           SkiaCanvas.tsx                                 │  │
│  │                                                           │  │
│  │  <Canvas>                                                │  │
│  │    {colorMatrix ? (                                      │  │
│  │      <ColorMatrix matrix={colorMatrix}>                  │  │
│  │        <SkiaImage image={sourceImage} />                 │  │
│  │      </ColorMatrix>                                      │  │
│  │    ) : (                                                 │  │
│  │      <SkiaImage image={sourceImage} />                   │  │
│  │    )}                                                    │  │
│  │  </Canvas>                                               │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           GPU (Skia)                                     │  │
│  │                                                           │  │
│  │  For each pixel (R, G, B, A):                            │  │
│  │                                                           │  │
│  │  R' = R*m[0] + G*m[1] + B*m[2] + A*m[3] + m[4]          │  │
│  │  G' = R*m[5] + G*m[6] + B*m[7] + A*m[8] + m[9]          │  │
│  │  B' = R*m[10] + G*m[11] + B*m[12] + A*m[13] + m[14]     │  │
│  │  A' = R*m[15] + G*m[16] + B*m[17] + A*m[18] + m[19]     │  │
│  │                                                           │  │
│  │  ⚡ GPU-accelerated, 60 FPS                              │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                  │
│                              ▼                                  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │ Rendered Image
                               ▼
                        ┌──────────────┐
                        │   Display    │
                        │   & Export   │
                        └──────────────┘
```

## Data Flow

### 1. Filter Selection Flow

```
User Taps Filter
       │
       ▼
FilterToolModal
       │
       ├─ Check isPro
       │     │
       │     ├─ If locked → Navigate to Paywall
       │     └─ If unlocked → Continue
       │
       ▼
setSelectedFilter(filterId)
       │
       ▼
Haptic Feedback
       │
       ▼
Visual Update (border highlight)
```

### 2. Intensity Adjustment Flow

```
User Taps Intensity Button (e.g., 70%)
       │
       ▼
setIntensity(0.7)
       │
       ▼
filter.getColorMatrix(0.7)
       │
       ├─ identity matrix (no effect)
       │        │
       │        ▼
       │  lerpMatrix(identity, filterMatrix, 0.7)
       │        │
       │        ▼
       └─ interpolated matrix
              │
              ▼
       Real-time Preview Update
```

### 3. Apply Filter Flow

```
User Taps "Apply Filter"
       │
       ▼
onApply(filterId, intensity)
       │
       ▼
useEditorStore.applyFilter({
  id: filterId,
  type: filterId,
  intensity: intensity
})
       │
       ├─ Update appliedFilter state
       ├─ Add to history (undo/redo)
       └─ Trigger re-render
              │
              ▼
       SkiaCanvas re-renders
              │
              ▼
       getFilterColorMatrix(appliedFilter)
              │
              ▼
       <ColorMatrix> applies transformation
              │
              ▼
       GPU processes pixels
              │
              ▼
       Filtered image displayed
```

### 4. Export Flow

```
User Taps Export
       │
       ▼
imageExporter.exportImage()
       │
       ▼
Capture Skia Canvas
       │
       ├─ Includes ColorMatrix transformation
       ├─ Includes all canvas elements
       └─ Renders at full resolution
              │
              ▼
       Save to file
              │
              ▼
       Share or save to Photos
```

## Component Relationships

```
┌─────────────────────────────────────────────────────────────┐
│                      EditorScreen                           │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                   Toolbar                              │ │
│  │  [Undo] [Redo] [Text] [Sticker] [Filters] [Export]    │ │
│  └────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           │ Tap Filters                      │
│                           ▼                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              FilterToolModal                           │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  Filter Grid (3 columns)                         │  │ │
│  │  │  [BW] [Sepia] [Vintage] [Cool] [Warm] [🔒]      │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  Intensity Controls                              │  │ │
│  │  │  [30%] [50%] [70%] [100%]                        │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  │  [Apply Filter]                                        │ │
│  └────────────────────────────────────────────────────────┘ │
│                           │                                  │
│                           │ onApply                          │
│                           ▼                                  │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              SkiaCanvas                                │ │
│  │  ┌──────────────────────────────────────────────────┐  │ │
│  │  │  <Canvas>                                        │  │ │
│  │  │    <ColorMatrix matrix={...}>                    │  │ │
│  │  │      <SkiaImage image={sourceImage} />           │  │ │
│  │  │    </ColorMatrix>                                │  │ │
│  │  │    {canvasElements.map(el => ...)}               │  │ │
│  │  │  </Canvas>                                       │  │ │
│  │  └──────────────────────────────────────────────────┘  │ │
│  └────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

## State Management

```
┌─────────────────────────────────────────────────────────────┐
│                    useEditorStore                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  State:                                                     │
│  ├─ canvasElements: CanvasElement[]                         │
│  ├─ selectedElementIds: string[]                            │
│  ├─ appliedFilter: ImageFilter | null  ◄─── Filter State   │
│  ├─ history: EditorHistory[]                                │
│  └─ historyIndex: number                                    │
│                                                             │
│  Actions:                                                   │
│  ├─ applyFilter(filter: ImageFilter)                        │
│  │    ├─ Set appliedFilter                                  │
│  │    ├─ Add to history                                     │
│  │    └─ Trigger re-render                                  │
│  │                                                           │
│  ├─ removeFilter()                                          │
│  │    ├─ Set appliedFilter = null                           │
│  │    └─ Add to history                                     │
│  │                                                           │
│  ├─ undo()                                                  │
│  │    └─ Restore previous appliedFilter                     │
│  │                                                           │
│  └─ redo()                                                  │
│       └─ Restore next appliedFilter                         │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Filter Processing Pipeline

```
Input: filterId = 'sepia', intensity = 0.8
       │
       ▼
┌──────────────────────────────────────┐
│  Step 1: Get Filter Definition       │
│  getFilterById('sepia')               │
│  → ColorFilter object                 │
└──────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Step 2: Generate Base Matrix        │
│  BASE_FILTER_MATRICES['sepia']       │
│  → composeMatrices(...)               │
│  → [20 numbers]                       │
└──────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Step 3: Apply Intensity              │
│  mixMatrices(identity, base, 0.8)    │
│  → interpolated matrix                │
└──────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Step 4: Apply to Image               │
│  <ColorMatrix matrix={...}>           │
│    <SkiaImage />                      │
│  </ColorMatrix>                       │
└──────────────────────────────────────┘
       │
       ▼
┌──────────────────────────────────────┐
│  Step 5: GPU Processing               │
│  For each pixel:                      │
│    R' = matrix transform              │
│    G' = matrix transform              │
│    B' = matrix transform              │
│    A' = matrix transform              │
└──────────────────────────────────────┘
       │
       ▼
Output: Filtered Image (60 FPS)
```

## Performance Characteristics

```
┌─────────────────────────────────────────────────────────────┐
│                    Performance Metrics                      │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Filter Selection:        < 16ms  (instant)                 │
│  Matrix Generation:       < 1ms   (CPU)                     │
│  GPU Processing:          16.67ms (60 FPS)                  │
│  Memory per Filter:       160 bytes                         │
│  Total Filters Memory:    ~2.2 KB (14 filters)              │
│  Export Overhead:         0ms (automatic)                   │
│                                                             │
│  ✅ Real-time preview at 60 FPS                             │
│  ✅ Zero network latency (offline)                          │
│  ✅ GPU-accelerated transformations                         │
│  ✅ Minimal memory footprint                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

## Integration Points

```
┌─────────────────────────────────────────────────────────────┐
│                    System Integration                       │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  Editor Screen                                              │
│  ├─ Toolbar button triggers FilterToolModal                 │
│  └─ Displays filtered canvas                                │
│                                                             │
│  Skia Canvas                                                │
│  ├─ Reads appliedFilter from store                          │
│  ├─ Generates ColorMatrix                                   │
│  └─ Applies to source image                                 │
│                                                             │
│  Export System                                              │
│  ├─ Captures Skia canvas (includes filter)                  │
│  └─ No additional processing needed                         │
│                                                             │
│  History System                                             │
│  ├─ Tracks filter changes                                   │
│  ├─ Supports undo/redo                                      │
│  └─ Persists across sessions                                │
│                                                             │
│  Paywall System                                             │
│  ├─ Checks isPro status                                     │
│  ├─ Locks premium filters                                   │
│  └─ Navigates to purchase flow                              │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

This architecture ensures:

- ⚡ High performance (60 FPS)
- 🎨 Professional quality filters
- 💰 Clear monetization path
- 🔄 Seamless undo/redo
- 📤 Automatic export integration
- 🧪 Easy testing and extension
